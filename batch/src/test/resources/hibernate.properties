# jdbc connection
hibernate.connection.driver_class = org.h2.Driver
hibernate.connection.url = jdbc:h2:batch/target/data/repository

# dialect
hibernate.dialect = org.hibernate.dialect.H2Dialect

# other
hibernate.show_sql = true
